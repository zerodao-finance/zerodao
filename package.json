{
  "name": "zerodao",
  "version": "0.1.0",
  "description": "zeroDAO monorepo",
  "repository": "https://github.com/zerodao-finance/zerodao",
  "scripts": {
    "contracts": "yarn workspace @zerodao/contracts",
    "protocol-old": "yarn workspace @zerodao/protocol"
  },
  "author": "zeroDAO",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "prettier": {
    "overrides": [
      {
        "files": "*.sol",
        "options": {
          "tabWidth": 2,
          "printWidth": 120,
          "bracketSpacing": true
        }
      }
    ]
  },
  "devDependencies": {
    "lerna": "^5.4.2"
  }
}
